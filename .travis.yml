stages:
  - build
  - test

jobs:
  include:
    - stage: build
      script: ./gradlew assemble
    - stage: test
      script: ./gradlew test
    - stage: test
      script: ./gradlew checkstyleMain checkstyleTest
