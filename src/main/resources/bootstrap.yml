spring:
  application:
    name: inventory-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      password: foo
      username: user
  data: # move to config server
    mongodb:
      uri: mongodb://inventory-mongodb:27017/inventory
