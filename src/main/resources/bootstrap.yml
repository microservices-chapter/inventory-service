spring:
  application:
    name: inventory-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      password: foo
      username: user
  data:
    mongodb:
      uri: mongodb://localhost:27017/inventory
